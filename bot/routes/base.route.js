'use strict';

let UserInput = require('../models/user_input.model.js');

module.exports = (bot) => {

    //bot.onText(/\/(.+) (.+)/, (msg, match) => {
    //    let cmd = match[1];
    //    let input = match[2];
    //    let userInput = new UserInput(msg, cmd, input);
    //
    //    return playerController.join(bot, userInput);
    //});

};